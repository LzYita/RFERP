# ERP 项目 Agent 协作规则

这份文件放在 ERP 仓库根目录。它服务于 Agent，不替代项目自身的开发文档。项目所有者的当前指令优先于本文件；如果指令涉及隐私、删除、远程写入或生产数据，必须先确认具体范围。

## 项目边界

- 本项目是个人/家庭使用的 ERP，默认按单一 Private GitHub 仓库管理。
- Git 管理代码、结构定义、迁移脚本、文档和脱敏示例；不管理真实家人数据。
- 不读取、提交或上传真实数据库、数据库导出、附件、日志、密钥、令牌、私钥和本地凭据。
- 运行数据和临时备份必须位于仓库之外；需要备份时使用项目规定的本地脚本或数据库原生导出工具。
- 不输出、复制或在日志中记录敏感值。发现疑似敏感文件时立即停止并报告路径，不把内容贴入对话。

## 开始任何工作前

1. 确认当前目录是 ERP 仓库，而不是备份目录或其他项目。
2. 执行并报告：

   ```powershell
   git status --short
   git branch --show-current
   git remote -v
   ```

3. 说明将修改的功能范围和预计文件；发现未解释的既有改动时，先保护它们，不覆盖、不清理。
4. 检查 `.gitignore` 是否排除了真实数据库、本地配置、日志和临时目录。

## 修改与验证

- 只修改用户授权的范围，不顺手重构无关模块。
- 修改后运行与改动相关的测试、类型检查或构建命令，并报告成功、失败和未执行项。
- 修改前后都检查 `git diff --stat`；需要时展示完整 diff。
- 业务行为、数据结构或部署方式发生变化时，同时更新项目中对应的说明文档和迁移说明。
- 不把测试通过等同于业务验收；涉及真实数据、权限或外部服务时，说明仍需由用户验收的部分。

## 分支规则

- `main` 是稳定分支，默认不直接在其上开发新功能。
- 新功能、修复和实验使用清晰命名的功能分支，例如 `feat/inventory`、`fix/empty-order`。
- 单人开发也尽量使用功能分支；多人协作通过 Pull Request 合并到 `main`。
- 不擅自创建、删除或切换到与用户当前工作无关的分支。
- 不强制覆盖远程分支，不使用 `git push --force` 或 `--force-with-lease`，除非用户明确说明目标分支、原因和回滚方案。

## Commit 与 Push 确认门

### 可以直接做的事

- 读取状态、diff、日志、分支和远程配置；
- 修改已授权的项目文件；
- 运行测试、构建和静态检查；
- 准备提交说明，但不自动提交；
- 在本地生成可审阅的 patch 或报告。

### 提交前必须展示

- 将要提交的文件清单；
- `git diff --cached --stat` 和必要的 diff 摘要；
- 测试/构建结果；
- 当前分支、目标远程和提交信息。

### 推送前必须获得明确授权

自然语言确认即可，但必须包含清晰意图。例如：

> 这次改动可以提交并推送到 origin 的 feat/inventory 分支。

或：

> 检查结果没问题，请提交当前变更并推送到朋友主仓库的 Pull Request 分支。

以下话语不能单独视为允许 commit/push：

- “继续”；
- “执行”；
- “好的”；
- “处理一下”；
- 只说“确认”但没有说明操作范围和目标。

得到授权后，仍只执行已展示的文件范围、提交信息、远程和分支。若执行过程中范围变化，必须停下重新说明。

## 高风险操作

以下操作必须单独说明准确目标、影响和恢复方式，并等待明确确认：

- `git restore` 丢弃未提交改动；
- `git reset --hard`、`git clean -fd` 或任何批量删除；
- `git revert` 涉及已经公开的业务数据或迁移；
- 删除分支、标签、远程仓库或 Pull Request；
- 强制推送、改写公共历史；
- 数据库迁移、生产数据写入、外部部署或发布。

默认优先使用可恢复方案：保存 patch、创建备份、用新提交修复，而不是改写历史或直接删除。

## 数据与凭据

- 真实数据库文件、导出文件、附件和运行日志不进入 Git；不要用 `git add -f` 绕过 `.gitignore`。
- `.env`、令牌、密码、私钥和个人配置不进入 Git。可以提交 `.env.example`，但只能放字段名和空值/示例值。
- 如果发现敏感信息已经被暂存或提交，立即停止推送，报告提交范围；不要自行把密钥继续传播到聊天、日志或新文件。
- GitHub 仓库默认 Private；涉及 Fork 时确认仓库所有者已经授予访问权限。

## 失败与不确定状态

- 命令失败、远程状态不明、认证异常或出现冲突时，保留现场并报告完整的非敏感错误信息。
- 不要为了重试而连续执行可能重复写入的命令；先判断上一次是否已经生效。
- 不要猜测远程仓库、目标分支、数据库位置或用户意图。
- 如果无法证明文件范围、测试结果或推送结果，标记为“未验证”，不要声称成功。

## 交接格式

每次工作结束时，按以下顺序报告：

1. 做了什么；
2. 改了哪些文件；
3. 运行了哪些测试/检查及结果；
4. 是否创建了 commit，commit 哈希是什么；
5. 是否 push，目标远程/分支是什么；
6. 哪些操作没有执行；
7. 用户下一步需要验收什么。

